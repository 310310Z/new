services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./my-nextjs-app:/app
    environment:
      - NODE_ENV=production
    command: npm start
